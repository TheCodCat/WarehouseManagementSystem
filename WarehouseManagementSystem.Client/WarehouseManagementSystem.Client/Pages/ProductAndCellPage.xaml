<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WarehouseManagementSystem.Client.Pages.ProductAndCellPage"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:converts="clr-namespace:WarehouseManagementSystem.Client.Converts"
             xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
             xmlns:components="clr-namespace:WarehouseManagementSystem.Client.Components"
             xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
             Title="Получение ячейки хранения">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ScrollView>
            <FlexLayout BindableLayout.ItemsSource="{Binding ShipmentResponce.Responce}"
        Direction="Row"
        Wrap="Wrap"
        JustifyContent="Start"
        AlignItems="Start"
        Margin="10">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="2">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="3"/>
                            </Border.StrokeShape>
                            <VerticalStackLayout>
                                <Label Text="{Binding Id, StringFormat='Номер заявки - {0}'}"/>
                                <BoxView StyleClass="Divider"/>
                                <Label Text="{Binding Party.Count, StringFormat='Количество - {0}'}"/>
                                <BoxView StyleClass="Divider"/>
                                <Label Text="{Binding Party.Product.Name, StringFormat='Название - {0}'}"/>
                                <BoxView StyleClass="Divider"/>
                                <Label Text="{Binding Party.Product.Description}"/>
                                <Label Text="{Binding CellTitle}" HorizontalTextAlignment="Center" FontSize="24" FontAttributes="Bold"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
        </ScrollView>

        <material:ButtonView Margin="20" VerticalOptions="End" HorizontalOptions="End" HeightRequest="50" WidthRequest="50"
        TappedCommand="{Binding PopModalNextCommand}">
            <Image>
                <Image.Source>
                    <FontImageSource FontFamily="FASolid" Glyph="{x:Static fa:Solid.AngleRight}"/>
                </Image.Source>
            </Image>
        </material:ButtonView>
    </Grid>
</ContentPage>